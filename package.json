{
  "name": "electron_class_schedule",
  "version": "1.0.0",
  "description": "classSchedule",
  "main": "main.js",
  "author": "lsl",
  "license": "GPLv3",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . classSchedule --platform=win32 --arch=x64 --out=./out --app-version=0.0.1 --overwrite"
  },
  "devDependencies": {
    "electron": "^28.1.1",
    "electron-packager": "^17.1.2"
  },
  "dependencies": {
    "electron-prompt": "^1.7.0"
  }
}
